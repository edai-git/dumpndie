{
  "name": "dumpndie-nodejs",
  "version": "1.1.0",
  "description": "dump and die, debugging helper function that prints each parameter to the console and then stops script execution or no",
  "main": "dist/index.js",
  "author": "edycpp@gmail.com",
  "license": "MIT",
  "private": false,
  "scripts": {
    "build": "rm -rf dist && npx tsc"
  },
  "devDependencies": {
    "@types/node": "^20.11.30",
    "typescript": "^5.4.3"
  }
}
